// Dialogs opened from the actions chapter, merged into DetailDialog by field-guide-page.jsx.
// A cell calls openDialog({ type: '...' }) and never imports the dialog component itself.
export const dialogViews = {};
